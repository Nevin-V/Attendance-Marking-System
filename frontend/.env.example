# Frontend environment variables — copy to .env.production and fill in real values
# Set this in Vercel Dashboard → Project → Settings → Environment Variables

# Your Render.com backend URL (NO trailing slash on /api/)
VITE_API_BASE_URL=https://scan-mark-api.onrender.com/api/
